<!-- 为了最简化，将控件修改为使用动态资源 -->
<UserControl x:Class="MusicPlayerApp.UserControls.PlaylistItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicPlayerApp.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="800">
    <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource MaterialDesignDivider}" Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- 专辑封面 -->
            <Border Width="50" Height="50" CornerRadius="3" BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}">
                <Image Source="{Binding AlbumArt}" Stretch="UniformToFill"/>
            </Border>
            
            <!-- 歌曲信息 -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="{DynamicResource MaterialDesignBody}"/>
                <TextBlock Text="{Binding ArtistName}" FontSize="12" Opacity="0.7" Foreground="{DynamicResource MaterialDesignBody}"/>
            </StackPanel>
            
            <!-- 操作按钮 -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding PlayCommand}">
                    <materialDesign:PackIcon Kind="Play" Foreground="{DynamicResource MaterialDesignBody}"/>
                </Button>
                <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding RemoveCommand}">
                    <materialDesign:PackIcon Kind="Delete" Foreground="{DynamicResource MaterialDesignBody}"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl> 