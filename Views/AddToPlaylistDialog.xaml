<Window x:Class="MusicPlayerApp.Views.AddToPlaylistDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="添加到播放列表" 
        Width="400" 
        Height="350" 
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{materialDesign:MaterialDesignFont}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="选择播放列表" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <!-- 播放列表列表 -->
        <ListView Grid.Row="1" 
                  x:Name="PlaylistsListView"
                  Margin="0,10"
                  BorderThickness="1"
                  BorderBrush="#333333">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Description}" FontSize="11" Opacity="0.7" 
                                      Margin="0,3,0,0" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        
                        <TextBlock Grid.Column="1" Text="{Binding SongsCount, Converter={StaticResource SongCountConverter}}" 
                                   VerticalAlignment="Center" Foreground="#666666"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!-- 按钮区域 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="创建新播放列表" 
                    Margin="0,0,10,0" 
                    Command="{Binding CreatePlaylistCommand}"
                    Style="{DynamicResource MaterialDesignOutlinedButton}"/>
            <Button Content="添加" 
                    IsDefault="True"
                    Command="{Binding ConfirmCommand}"
                    Style="{DynamicResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueMidBrush}"/>
        </StackPanel>
    </Grid>
</Window> 